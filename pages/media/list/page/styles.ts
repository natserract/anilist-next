import { createStyles } from '@material-ui/core/styles'

const styles = () =>
  createStyles({
    paginationContainer: {
      marginTop: 35,
      marginBottom: 20,
    }
  })

export default styles